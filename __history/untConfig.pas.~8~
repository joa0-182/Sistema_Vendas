unit untConfig;

interface

uses
  Winapi.Windows, Winapi.Messages, System.SysUtils, System.Variants, System.Classes, Vcl.Graphics,
  Vcl.Controls, Vcl.Forms, Vcl.Dialogs, Vcl.StdCtrls, Vcl.Imaging.pngimage,
  Vcl.ExtCtrls, Vcl.Buttons, untHomeTDI;

type
  TfrmConfig = class(TForm)
    Header: TPanel;
    Fundo: TPanel;
    pnlImg: TPanel;
    Image1: TImage;
    Label1: TLabel;
    btnSair: TSpeedButton;
    btnCancelar: TSpeedButton;
    pnlBotoes: TPanel;
    ladoDireito: TPanel;
    ladoEsquerdo: TPanel;
    pnlAlterUsu: TPanel;
    pnlIcon1: TPanel;
    imgUser: TImage;
    btnAlterarUsu: TSpeedButton;
    Panel1: TPanel;
    SpeedButton1: TSpeedButton;
    Panel2: TPanel;
    Image2: TImage;
    procedure btnSairClick(Sender: TObject);
    procedure btnCancelarClick(Sender: TObject);
    procedure btnAlterarUsuClick(Sender: TObject);
  private
    { Private declarations }
  public
    { Public declarations }
  end;

var
  frmConfig: TfrmConfig;

implementation

{$R *.dfm}

procedure TfrmConfig.btnSairClick(Sender: TObject);
begin
  frmConfig.Close;
end;

procedure TfrmConfig.btnAlterarUsuClick(Sender: TObject);
begin
  if AbrirFundo.execute then

end;

procedure TfrmConfig.btnCancelarClick(Sender: TObject);
begin
  frmConfig.Close;
end;

end.
